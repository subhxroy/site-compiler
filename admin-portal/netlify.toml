# Netlify Configuration for Standalone Admin Portal (Static Export)
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_FIREBASE_API_KEY"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
